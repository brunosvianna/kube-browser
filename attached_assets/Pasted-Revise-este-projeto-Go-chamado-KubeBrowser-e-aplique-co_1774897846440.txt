Revise este projeto Go chamado KubeBrowser e aplique correções de consistência e maturidade antes de uma divulgação pública mais forte.

Objetivo:
corrigir incoerências entre código, README, changelog e pipeline de release, sem reinventar o projeto e sem remover funcionalidades úteis.

Faça exatamente o seguinte:

1. Corrija a documentação da versão do Go
- Revise `README.md`, `CONTRIBUTING.md`, workflows e qualquer outro arquivo de documentação.
- Garanta que a versão mínima do Go esteja consistente com o `go.mod` e com os workflows do GitHub Actions.
- Se o projeto usa Go 1.25 no `go.mod` e nos workflows, o README não pode continuar dizendo Go 1.21.
- Deixe tudo alinhado.

2. Corrija o workflow de release para usar o CHANGELOG real
- Revise `.github/workflows/release.yml`.
- Se já existe etapa que extrai as notas da versão atual do `CHANGELOG.md`, faça a release usar esse conteúdo como corpo da release.
- Não deixe o processo depender apenas de `generate_release_notes: true` se já existe changelog curado manualmente.
- Se fizer sentido, mantenha `generate_release_notes` desabilitado ou apenas complementar, mas a release precisa refletir o conteúdo do `CHANGELOG.md`.

3. Alinhe o CHANGELOG com as releases públicas
- Revise `CHANGELOG.md` e o estado atual das releases/tags.
- Se houver versões no changelog que ainda não foram publicadas como release, não deixe isso parecer inconsistente.
- Aplique a solução mais correta e sustentável:
  - ou mover essas entradas para `[Unreleased]`,
  - ou ajustar o processo/documentação para refletir corretamente o estado atual.
- Não invente releases que não existem.
- Não apague histórico real sem necessidade.

4. Corrija a inconsistência de segurança no fallback `find`
- Revise a implementação que ainda usa `sh -c` com interpolação de path, especialmente na listagem com `find`.
- O changelog não pode afirmar que isso foi corrigido se o código ainda mantém esse comportamento.
- Faça uma das duas coisas:
  a) corrija de fato a implementação para evitar esse padrão inseguro, preferindo uma execução mais segura e previsível;
  b) se não for possível corrigir totalmente agora, ajuste o changelog e a documentação para não alegar uma correção que não ocorreu.
- Priorize corrigir o código de verdade.
- Garanta comportamento seguro para paths com caracteres especiais.

5. Revise mensagens/documentação para não prometer mais do que o projeto entrega
- O projeto é uma ferramenta local, com cliente Kubernetes mantido em memória no processo.
- Não apresente isso como solução multiusuário ou multi-sessão se o código não suporta isso.
- Ajuste README e documentação se houver qualquer linguagem ambígua nesse sentido.

6. Preserve o que já está bom
- Não remova CI
- Não remova changelog
- Não remova docs existentes
- Não remova helper pod, modo read-only ou demo
- Faça apenas as correções necessárias para aumentar a coerência e a maturidade do projeto

7. No final, me entregue:
- resumo do que foi alterado
- arquivos modificados
- explicação curta de cada correção
- qualquer ponto que você não conseguiu corrigir completamente
- sugestão de próximos passos

Regras:
- não invente funcionalidades
- não invente releases
- não invente correções de segurança
- não crie complexidade desnecessária
- prefira consistência, clareza e honestidade técnica
- mantenha o foco do projeto: ferramenta local para inspeção de PVCs em Kubernetes